[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]  # Pyflakes, pycodestyle errors, and isort
ignore = ["E501"]        # Let the formatter handle long lines automatically